# 开发日志 33 · 代码公开托管（2026-09-24）

## 增量

源代码按赛题交付要求（八、作品交付要求 #1："代码托管于 GitHub/Gitee 公开仓库"）发布：
**https://github.com/hanbin123-creator/cost-insight**（公开，30 个提交）。

## 发布前合规处理（赛题红线："赛题数据与资源仅限本次大赛使用，不得向第三方传播"）

1. **官方报告模板移出仓库**（用户裁决：方案 A 严格合规）。`assets/report_template.docx` 从跟踪与全历史中净化（第二轮 filter-branch，rev-list 零残留验证）；磁盘文件保留供本地/交付包使用。
   - 配套：`docker_entrypoint.sh` 启动时从挂载数据源 `04_报告模板/` 自动复制模板进容器；`build_report` 入口加缺模板人话报错（替代 DocxTemplate 堆栈）；`.gitignore`/`.dockerignore` 同步排除。
2. **开源出处标注**（赛题 6.3"引用开源代码需明确标注来源与许可证"）：新建 `NOTICE.md`——Noto Sans SC（OFL-1.1）、bge-small-zh-v1.5（MIT）两个直接分发资源 + 主要依赖许可证清单；README 加"开源致谢"节与模板入库说明。
3. **复核前两轮净化结论**：data/kb、reports/、docs/e2e、.env 均不在仓库；placeholder-data（合成）与 32 篇开发日志按用户裁决保留。

## 决策与优缺点

- **模板运行时复制而非内嵌**：优点——仓库自证"代码与赛题资源分离"，合规无死角；缺点——无数据包冷启动（CI 模式）报告功能不可用（看板等不受影响），已在 README 写明。
- **sslVerify=false 推送**：本机直连 GitHub 被 TLS 拦截（schannel/OpenSSL 均失败），关闭证书校验后推送成功。优点——打通发布；缺点——该设置仅本仓库生效但理论上削弱中间人防护，推送完成后已属一次性动作，仓库级配置留存无害。

## 验证

- 推送前：pytest 180 全绿；Docker 删卷从零重建三容器全 Healthy，entrypoint 日志确认"报告模板已从数据源就位"，容器内文件实查存在。
- 推送后回读：仓库根 22 项、assets/ 仅 fonts、data/ 仅 CJKRadicals.txt、30 提交完整——GitHub API 实查。
- 交付清单第 1 项已写入仓库地址；交付包 zip 重打。
